# 模型路由

模型路由用于决定不同 AI 任务使用哪个模型。你可以把规划、正文、审核、拆书、图片等任务分配给不同模型，以平衡质量、速度和成本。

## 什么时候打开

- 已经配置多个模型。
- 想降低长任务成本。
- 想让正文、规划或审核使用不同模型。

## 你可以在这里做什么

- 为不同任务选择模型。
- 调整默认模型。
- 测试不同模型在不同任务中的表现。

## 使用建议

第一次使用时可以先用一个模型跑通流程。等你知道哪些任务贵、哪些任务质量要求高，再细分模型路由。
